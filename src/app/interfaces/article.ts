export interface Article {
  id: string,
  brand_id: string,
  provider_id: string,
  designation: string,
  reference: string,
  description?: string,
  quantity?: string,
  priority: string,
  price?: string,
}
