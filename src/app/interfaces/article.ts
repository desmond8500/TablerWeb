export interface Article {
  name: string,
  reference: string,
  description?: string,
  priority: string,
  price?: string,
  quantity?: string,
  brand_id?: string,
  provider_id?: string,
  storage_id?: string,
}
